Upload the .ino code to your Arduino board.

# --- Running the Node ---
# Open 3 separate terminals.

# Terminal 1: Start ROS Master
roscore

# Terminal 2: Start the rosserial connection node.
# Replace /dev/ttyACM0 with your Arduino's serial port.
rosrun rosserial_python serial_node.py /dev/ttyACM0

# Terminal 3: Publish a message to toggle the LED.
rostopic pub /start_blink std_msgs/Empty "{}"
